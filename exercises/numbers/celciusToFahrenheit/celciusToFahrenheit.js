/**
 * Given a temperature in Celcius, return the temperature in Fahrenheit.
 *
 * @param {number} temp - The temperature in Celcius
 * @returns {boolean} The temperature in Fahrenheit
 */
function celciusToFahrenheit(temp) {
  // This is your job. :).
}

if (require.main === module) {
  console.log('Running sanity checks for celciusToFahrenheit:');

  // Your sanity checks go here.
}

module.exports = celciusToFahrenheit;
