/**
 * Given a temperature in Fahrenheit, return the temperature in celcius.
 *
 * @param {number} temp - The temperature in Fahrenheit
 * @returns {boolean} The temperature in celcius
 */
function fahrenheitToCelcius(temp) {
  // This is your job. :).
}

if (require.main === module) {
  console.log('Running sanity checks for fahrenheitToCelcius:');

  // Your sanity checks go here.
}

module.exports = fahrenheitToCelcius;
